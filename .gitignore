# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
.DS_Store

# SwiftPM local resolution artifacts (Package.resolved is fine to commit, but local caches aren't)
.swiftpm/
.build/

# Secrets — never commit real values
Menu/Secrets.swift

# Supabase CLI local cache (machine-specific, not secret but not useful to track)
supabase/.temp/
