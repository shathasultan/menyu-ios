# تشغيل منيو على جهازك

هذا الدليل لمن يريد تجربة التطبيق على ماك، دون حساب Apple Developer.
التطبيق يعمل في **محاكي الآيفون** المدمج مع Xcode — لا يحتاج جوالًا ولا اشتراكًا.

المدة المتوقعة: **ساعة تقريبًا**، أغلبها تحميل Xcode.

---

## ما تحتاجه

| | |
|---|---|
| جهاز | **ماك** فقط — لا يعمل على ويندوز |
| Xcode | أحدث إصدار من App Store · حوالي **١٥ جيجابايت** |
| ملف | `Secrets.swift` — يُرسل لك على حدة، ليس في المستودع |

> التطبيق مبني لنظام **iOS 26.5**، فيحتاج إصدار Xcode يدعمه. إن كان نظام الماك لديك قديمًا فلن تستطيع تثبيت هذا الإصدار — أخبرنا وسنرسل لك تسجيلًا مصوّرًا بدلًا منه.

---

## الخطوات

### ١. ثبّت Xcode

من **App Store** على الماك، ابحث عن `Xcode` وثبّته. التحميل كبير — ابدأه واتركه.

بعد التثبيت، افتحه مرة واحدة ووافق على الشروط، ثم أغلقه.

### ٢. نزّل المشروع

من صفحة المستودع على GitHub: **Code → Download ZIP**، ثم فك الضغط.

أو من الطرفية:

```
git clone https://github.com/shathasultan/menyu-ios.git
cd menyu-ios
```

### ٣. أضف ملف المفاتيح

المشروع لا يحتوي على مفاتيح قاعدة البيانات — تُرسل لك منفصلة عن الكود.

ضع الملف الذي وصلك باسم `Secrets.swift` داخل مجلد `Menu/`، بجوار `MenuApp.swift`.

المسار النهائي يكون:

```
menyu-ios/Menu/Secrets.swift
```

### ٤. افتح المشروع

اضغط ضغطة مزدوجة على **`Menu.xcodeproj`**.

سيبدأ Xcode بتحميل المكتبات التي يعتمد عليها التطبيق (Supabase وغيرها). انتظر حتى يتوقف شريط التقدم أعلى النافذة — قد يستغرق دقائق في المرة الأولى.

### ٥. اضبط التوقيع

المشروع محفوظ بحساب المطوّر الخاص بنا، ولن يتعرّف عليه جهازك. عدّله مرة واحدة:

1. اختر **Menu** من أعلى القائمة الجانبية اليسرى (الأيقونة الزرقاء)
2. افتح تبويب **Signing & Capabilities**
3. من قائمة **Team** اختر **None**

هذا كافٍ للمحاكي. لا يُطلب منك حساب ولا اشتراك.

### ٦. شغّل

1. من شريط الأعلى، اختر جهازًا من قائمة الأجهزة — مثل **iPhone 17 Pro**
2. اضغط زر التشغيل ▶︎ (أو `Cmd + R`)

أول تشغيل يستغرق دقائق لأنه يبني كل شيء من الصفر. بعدها يفتح المحاكي ويعمل التطبيق.

---

## إن واجهت مشكلة

| الرسالة | الحل |
|---|---|
| `Cannot find 'Secrets' in scope` | ملف `Secrets.swift` غير موجود أو في غير مكانه — راجع الخطوة ٣ |
| `No account for team NGY34GZ6UN` | لم تُغيّر الـ Team إلى None — راجع الخطوة ٥ |
| `Signing for "Menu" requires a development team` | نفس الحل أعلاه، وتأكد أن الجهاز المختار محاكٍ لا جوالًا حقيقيًا |
| توقف عند `Resolving Package Graph` | يحتاج اتصالًا بالإنترنت لتحميل المكتبات — تأكد من الشبكة وأعد المحاولة |
| `The run destination is not valid` | اختر محاكيًا من قائمة الأجهزة، لا `Any iOS Device` |

---

## ماذا تجرّب

الدورة كاملة تأخذ خمس دقائق:

1. اختر **«لدي متجر»** وسجّل مطعمًا جديدًا
2. أضف تصنيفًا مثل «مشروبات ساخنة» — لاحظ أنه يأخذ حرفًا يخصّه
3. أضف منتجًا تحته — الرمز `A01` يتكوّن وحده، ولا يمكن تعديله
4. ارجع لشاشة البداية، وادخل **«دخول الإدارة»**، واعتمد المطعم
5. بدّل لدور الزبون، وابحث عن المنتج برمزه
6. غيّر السعر أو علّم المنتج «نفذ» من شاشة التاجر، وانظر أثره عند الزبون

وجرّب أيضًا: زر اللغة أعلى الشاشة الرئيسية — التطبيق كامل بالعربية والإنجليزية.
